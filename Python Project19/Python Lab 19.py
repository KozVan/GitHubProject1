# numbers  = [2, 4, 6]
# languages = ['Python', 'Java']
# languages[1] = 'Python'

# mylist = []
# listik = list()

numbers = list(range(5)) # ------> numbers = [0, 1, 2, 3, 4]
even_numbers = list(range(0, 10, 2)) # ----> even_numbers = [0, 2, 4, 6, 8]
chars = list('abcde') # ------> chars = ['a', 'b', 'c', 'd', 'e']
print(numbers, even_numbers, chars)